Se debe de crear lo siguiente para la correcta ejecucion:

crear ambiente virutal
python -m venv .venv

activar el ambiente virutal
.\.venv\Scripts\activate

instalar pandas y matplotlib
pip install pandas, matplotlib
